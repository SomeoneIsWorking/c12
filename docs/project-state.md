# Project state

- Bare generic execution: partial. The shared whole-program profile builds, starts `SCUS_946.66`
  once, and yields at emitter-proven libetc VSync. The supplied USA CHD resolves through
  `SYSTEM.CNF` to that executable.
- Current runtime frontier: generated dispatch refuses `0x800A7F90` after the first VSync return.
  This is a generic emitter function-discovery gap, not a title frame-loop or VSync configuration
  problem; no C-12-specific fallback is installed.
